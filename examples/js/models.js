// Single source of truth for the 3 bundled STL files - shared by both the
// full-page playground instance and the separate contained-box demo.
export const models = ["/examples/models/pineapple.stl", "/examples/models/apple.stl", "/examples/models/banana.stl"];
